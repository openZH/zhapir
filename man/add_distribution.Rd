% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_distribution.R
\name{add_distribution}
\alias{add_distribution}
\title{Add a distribution to an existing dataset}
\usage{
add_distribution(
  auth_info,
  dataset_id,
  title,
  description,
  file_path = NULL,
  access_url_id = NULL,
  download_url_id = NULL,
  format_id = NULL,
  media_type_id = NULL,
  use_dev = TRUE,
  ...
)
}
\arguments{
\item{auth_info}{Authentication information from login_to_api()}

\item{dataset_id}{ID of the dataset to add the distribution to}

\item{title}{Title of the distribution}

\item{description}{Description of the distribution}

\item{file_path}{Optional path to a file to upload (if provided, the file will be uploaded)}

\item{access_url_id}{URL where the distribution can be accessed}

\item{download_url_id}{URL where the distribution can be downloaded}

\item{format_id}{ID of the format (e.g., CSV, XML)}

\item{media_type_id}{ID of the media type}

\item{use_dev}{Whether to use the development environment (default: TRUE)}

\item{...}{Additional distribution properties}
}
\value{
The created distribution information as returned by the API
}
\description{
Adds a new distribution (file or access point) to an existing dataset.
Optionally uploads a file to the distribution.
}
\examples{
\dontrun{
# First login
auth_info <- login_to_api("admin@example.com", "admin")

# Add a distribution to dataset with ID 123
add_distribution(
  auth_info,
  dataset_id = 123,
  title = "Air Quality CSV Data",
  description = "CSV file containing hourly air quality measurements"
)

# Add a distribution with a file upload
add_distribution(
  auth_info,
  dataset_id = 123,
  title = "Air Quality CSV Data",
  description = "CSV file containing hourly air quality measurements",
  file_path = "path/to/your/data.csv",
  format_id = 1,  # Assuming 1 is the ID for CSV format
  media_type_id = 2  # Assuming 2 is the ID for text/csv media type
)
}
}
